审查顺序：控制层（接口）->服务层（实现类）->数据访问层（数据结构，表设计）

生成相关接口(generate)

1.rewrite：
改成不是针对被选择的某条优化，而是综合当前图片的历史生成结果和历史修改命令，进行优化生成。

2.render：
不要把要渲染成图的文案自动放入历史记录。

3.history
不要使用store.json储存历史记录，用数据库储存！让每个用户有自己的历史记录。

4.hot-topics：
改成从本地文件读取。

5.获取额度：
如果是游客，额度返回-1。

