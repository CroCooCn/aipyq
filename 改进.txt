1.去掉收藏功能，只保留历史记录功能，历史记录像当前这样即可，去掉所有“收藏”相关按钮和展示功能。
2.去掉“解析图片”按钮，直接就是“生成文案”，不用显示标签和图片内容了，生成文案可以选择使用/不用深度思考。（见"ai调用规范.txt"）
3.在生成好文案以后，用户可以输入修改指令，比如用户输入“改更文雅一点”，那么下次生成的文案就会更文雅，下次生成的文案在之前生成文案的后面显示，而不是直接盖掉。
4.调整“渲染成图”前端显示在文案生成部分的右边，也就是说，网页的左边用列表的形式显示生成的文案，生成的历史文案，左边最下面是用户输入修改指令的地方，网页右边显示渲染成图的结果。
