1.不要rewrite和copy两个生成方式，只保留copy一个，传入参数：
图片URL(必须)
图片样式指令(可选)
当前上下文之前的对话历史(初始为空)

其中，对话历史保存在redis中：
用:当前轮次对话的id，作为索引，内容是“样式指令-生成结果”的键值对。用户刷新网页，轮次就应该换掉。

2.把生成（文案生成，图片渲染）用的提示词专门放到一个文件里，便于从外部修改。

3.确保前后端配合一致。

